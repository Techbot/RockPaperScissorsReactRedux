# behat.yml

default:
    suites:
        Dice_features:
            paths:    [ %paths.base%/features/Dice ]
            contexts: [ DiceContext ]

        PRS_features:
            paths:    [ %paths.base%/features/RPS ]
            contexts: [ PRSContext ]
